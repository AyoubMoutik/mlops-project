\documentclass[11pt,a4paper]{article}

\usepackage[margin=1.85cm,top=2.1cm,bottom=2.2cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{float}
\usepackage{needspace}
\usepackage{hyperref}
\usepackage{listings}
\usepackage[most]{tcolorbox}

\definecolor{navy}{HTML}{0F172A}
\definecolor{slate}{HTML}{475569}
\definecolor{muted}{HTML}{64748B}
\definecolor{line}{HTML}{CBD5E1}
\definecolor{soft}{HTML}{F8FAFC}
\definecolor{blue}{HTML}{2563EB}
\definecolor{teal}{HTML}{0D9488}
\definecolor{amber}{HTML}{D97706}
\definecolor{codebg}{HTML}{F1F5F9}

\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    urlcolor=teal,
    citecolor=blue,
    pdftitle={MLOps Exam Project Report},
    pdfauthor={Ayoub Moutik}
}

\pagestyle{fancy}
\fancyhf{}
\lhead{\textcolor{slate}{MLOps Exam Project}}
\rhead{\textcolor{slate}{MLOps Workflow Report}}
\cfoot{\textcolor{slate}{\thepage}}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{line}\leaders\hrule height \headrulewidth\hfill}}

\titleformat{\section}
  {\Large\bfseries\color{navy}}
  {\thesection}{0.8em}{}

\titleformat{\subsection}
  {\large\bfseries\color{navy}}
  {\thesubsection}{0.7em}{}

\titleformat{\subsubsection}
  {\normalsize\bfseries\color{navy}}
  {\thesubsubsection}{0.6em}{}

\setlist[itemize]{leftmargin=1.4em,itemsep=0.25em,topsep=0.25em}
\setlist[enumerate]{leftmargin=1.5em,itemsep=0.25em,topsep=0.25em}
\renewcommand{\arraystretch}{1.25}
\setcounter{tocdepth}{3}

\lstdefinestyle{projectcode}{
    backgroundcolor=\color{codebg},
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue}\bfseries,
    commentstyle=\color{muted},
    stringstyle=\color{teal},
    breaklines=true,
    frame=single,
    rulecolor=\color{line},
    frameround=tttt,
    columns=fullflexible,
    showstringspaces=false,
    tabsize=2
}

\newtcolorbox{infobox}[1]{
    enhanced,
    colback=soft,
    colframe=line,
    coltitle=navy,
    fonttitle=\bfseries,
    title=#1,
    arc=2mm,
    boxrule=0.6pt,
    left=3mm,
    right=3mm,
    top=2mm,
    bottom=2mm
}

\begin{document}

% ---------------- Cover page ----------------
\begin{titlepage}
    \thispagestyle{empty}

    \noindent
    \begin{minipage}[t]{0.48\textwidth}
        \IfFileExists{logos/bdiologo.png}
            {\includegraphics[height=2cm,width=1.6cm]{logos/bdiologo.png}}
            {\fbox{\rule{0pt}{2cm}\hspace{1cm}BDIO\hspace{1cm}}}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \raggedleft
        \IfFileExists{logos/ensamlogo.png}
            {\includegraphics[height=1.6cm]{logos/ensamlogo.png}}
            {\fbox{\rule{0pt}{1.6cm}\hspace{1cm}ENSAM\hspace{1cm}}}
    \end{minipage}

    \vspace{2.4cm}

    \begin{center}
        {\small DevOps \& MLOps Project}

        \vspace{0.65cm}

        \begin{tcolorbox}[
            enhanced,
            width=0.86\textwidth,
            colback=white,
            colframe=navy,
            arc=2mm,
            boxrule=0.9pt,
            left=7mm,
            right=7mm,
            top=6mm,
            bottom=6mm
        ]
            \centering
            {\Large\bfseries\color{navy} End-to-End MLOps Workflow}\\[0.25cm]
            {\normalsize\color{slate} CI/CD, Dockerized Training, Model Serving, and Monitoring}\\[0.35cm]
            {\small\color{muted} Text Classification System using Jenkins, Ray, MLflow, FastAPI, Prometheus, and Grafana}
        \end{tcolorbox}
    \end{center}

    \vspace{2.5cm}

    \noindent
    \begin{minipage}[t]{0.42\textwidth}
        {\small Prepared by:}

        \vspace{0.35cm}

        {\small Omar Elkhali}

        \vspace{0.25cm}

        {\small Ayoub Moutik}

        \vspace{0.25cm}

        {\small Jamila Ouzzane}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.32\textwidth}
        {\small Supervised by:}

        \vspace{0.35cm}

        {\small Prof. HIRCHOUA Bader}


    \end{minipage}

    \vfill

    \begin{center}
        {\small Academic Year: 2025--2026}
    \end{center}

\end{titlepage}

\tableofcontents
\newpage

\section*{Abstract}
\addcontentsline{toc}{section}{Abstract}

This report presents the design and implementation of an MLOps workflow for a machine learning text classification project. The system is organized around a modular Python codebase that supports data processing, model training, evaluation, prediction, serving, CI/CD automation, and monitoring. The workflow emphasizes maintainability, traceability, reproducibility, and operational visibility, which are essential requirements for reliable machine learning systems.

The report begins by describing the transition from notebook-based experimentation to script-based engineering. It then presents the logging and reproducibility mechanisms provided by Git, dependency management, MLflow tracking, model checkpointing, dataset organization, and deterministic utilities. The final sections explain the Dockerized Jenkins CI/CD pipeline, the deployment gate based on model quality, and the local monitoring stack built with FastAPI, Prometheus, and Grafana.

\section{Project Context And Objective}

\subsection{Context}

The project material is composed of five course PDFs and two repositories:

\begin{itemize}
    \item \texttt{MLOpsFull}: the main machine learning application.
    \item \texttt{Monitoring-ML}: a monitoring notebook and README covering model performance, drift detection, and online monitoring.
\end{itemize}

The main application is a text classification system inspired by the Made With ML project. It classifies machine learning project descriptions into tags such as computer vision, natural language processing, graph learning, MLOps, reinforcement learning, and other categories.

\subsection{Objective}

The objective is to demonstrate an end-to-end MLOps workflow. The project includes the foundational engineering practices required before automation and monitoring can be introduced reliably:

\begin{enumerate}
    \item moving from notebooks to scripts;
    \item logging for ML systems;
    \item reproducibility through versioned code, data, models, and dependencies.
\end{enumerate}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/repository_snapshot.png}
    \caption{Current project material available in the workspace.}
    \label{fig:repo-snapshot}
\end{figure}

\section{Building The MLOps Workflow}

\subsection{Part 1: Moving From Notebooks To Scripts}

\subsubsection{Purpose}

Notebooks are excellent for exploration because they allow interactive execution, visual inspection, and fast experimentation. However, they are not ideal as the final structure of a production ML project. A notebook can hide execution order, retain variables in memory, and make testing or automation difficult.

Scripts provide a cleaner engineering structure. They make workloads easier to test, execute from a terminal, reuse in CI/CD, and deploy as services. This is the first major step from data science experimentation toward MLOps.

\subsubsection{Existing Notebook}

The original notebook is:

\begin{lstlisting}[style=projectcode]
MLOpsFull/mlops_Mine - Copy.ipynb
\end{lstlisting}

Its main sections include setup, data ingestion, preprocessing, training, experiment tracking, evaluation, interpretability, prediction, and serving. This is a typical research-to-development notebook: it contains the full workflow, but the logic is not yet presented as independent production components.

\subsubsection{Script-Based Architecture}

The notebook workflow has been separated into modules under \texttt{MLOpsFull/madewithml}. Each file owns a specific responsibility, which improves maintainability and makes the project easier to automate.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth]{figures/code_architecture.png}
    \caption{Script organization of the MLOpsFull project.}
    \label{fig:architecture}
\end{figure}

\subsubsection{Module Responsibilities}

\begin{longtable}{p{0.27\textwidth} p{0.63\textwidth}}
\toprule
\textbf{Module} & \textbf{Responsibility} \\
\midrule
\texttt{config.py} & Defines shared project configuration, logging paths, MLflow tracking URI, and constants such as stopwords. \\
\texttt{data.py} & Loads datasets, performs stratified splitting, cleans text, tokenizes input, and applies preprocessing. \\
\texttt{models.py} & Defines the PyTorch model architecture used for text classification. \\
\texttt{train.py} & Contains the Ray Train workload, training loop, checkpointing, and MLflow callback integration. \\
\texttt{tune.py} & Contains the hyperparameter tuning workload using Ray Tune and HyperOpt. \\
\texttt{evaluate.py} & Computes overall, per-class, and slice-level metrics. \\
\texttt{predict.py} & Loads model checkpoints and performs prediction or probability estimation. \\
\texttt{serve.py} & Defines the FastAPI application and Ray Serve deployment. \\
\texttt{utils.py} & Provides shared utility functions such as seed setting, batching, and dictionary saving. \\
\bottomrule
\end{longtable}

\subsubsection{Notebook-To-Script Mapping}

\begin{table}[H]
\centering
\begin{tabularx}{0.96\textwidth}{>{\bfseries}p{0.34\textwidth} X}
\toprule
Notebook Area & Script Destination \\
\midrule
Data loading and preprocessing & \texttt{madewithml/data.py} \\
Model definition & \texttt{madewithml/models.py} \\
Training loop & \texttt{madewithml/train.py} \\
Hyperparameter search & \texttt{madewithml/tune.py} \\
Evaluation & \texttt{madewithml/evaluate.py} \\
Prediction & \texttt{madewithml/predict.py} \\
Serving & \texttt{madewithml/serve.py} \\
Shared configuration & \texttt{madewithml/config.py} \\
Shared helpers & \texttt{madewithml/utils.py} \\
\bottomrule
\end{tabularx}
\caption{Mapping from notebook sections to production-oriented scripts.}
\end{table}

\subsubsection{Benefits For MLOps}

This structure is important because Jenkins, tests, model training jobs, and deployment commands all need scriptable entry points. A CI/CD server cannot reliably execute arbitrary notebook cells in a human-controlled order. It needs deterministic commands and modules.

The current organization creates a foundation for:

\begin{itemize}
    \item command-line training;
    \item automated evaluation;
    \item experiment tracking;
    \item API deployment;
    \item future Jenkins automation.
\end{itemize}

\subsection{Part 2: Logging For ML Systems}

\subsubsection{Purpose}

Logging records what happens inside the application during execution. In ML systems, logging is essential because failures can come from code, data, training configuration, model artifacts, infrastructure, or serving inputs.

Compared with \texttt{print}, logging is more suitable because it supports severity levels, multiple outputs, timestamps, persistent files, and consistent formatting.

\subsubsection{Logging Configuration In The Project}

The logging setup is defined in:

\begin{lstlisting}[style=projectcode]
MLOpsFull/madewithml/config.py
\end{lstlisting}

The project creates a \texttt{logs} directory and defines several handlers:

\begin{itemize}
    \item a console handler for terminal output;
    \item an \texttt{info.log} rotating file handler for informative logs;
    \item an \texttt{error.log} rotating file handler for errors.
\end{itemize}

Key implementation pattern:

\begin{lstlisting}[style=projectcode,language=Python]
LOGS_DIR = Path(ROOT_DIR, "logs")
LOGS_DIR.mkdir(parents=True, exist_ok=True)

logging_config = {
    "version": 1,
    "disable_existing_loggers": False,
    "handlers": {
        "console": {...},
        "info": {...},
        "error": {...},
    },
    "root": {
        "handlers": ["console", "info", "error"],
        "level": logging.INFO,
    },
}

logging.config.dictConfig(logging_config)
logger = logging.getLogger()
\end{lstlisting}

\subsubsection{Where Logs Are Used}

The project uses \texttt{logger.info(...)} to record important workflow outputs. For example:

\begin{itemize}
    \item training logs the timestamp, run ID, parameters, and metric history;
    \item evaluation logs overall metrics, per-class metrics, and slice metrics;
    \item prediction logs model prediction results.
\end{itemize}

\begin{figure}[H]
    \centering
    \begin{tcolorbox}[
        enhanced,
        width=0.96\textwidth,
        colback=soft,
        colframe=soft,
        boxrule=0pt,
        arc=1mm,
        left=7mm,
        right=7mm,
        top=6mm,
        bottom=7mm
    ]
        {\Large\bfseries\color{navy} Implemented Foundations Before CI/CD}\\[0.15cm]
        {\small\color{slate} Evidence already present in the cloned codebase.}\\[0.8cm]

        \begin{minipage}[t]{0.30\textwidth}
            \begin{tcolorbox}[
                enhanced,
                height=5.6cm,
                colback=white,
                colframe=blue,
                coltitle=blue,
                fonttitle=\bfseries,
                title=Logging,
                arc=2mm,
                boxrule=0.8pt,
                left=3mm,
                right=3mm,
                top=2mm,
                bottom=2mm
            ]
                \small\raggedright
                \begin{itemize}[leftmargin=1em,itemsep=0.35em]
                    \item \texttt{LOGS\_DIR} configured
                    \item \texttt{info.log} rotating handler
                    \item \texttt{error.log} rotating handler
                    \item \texttt{logger.info(...)} in core workflows
                \end{itemize}
            \end{tcolorbox}
        \end{minipage}
        \hfill
        \begin{minipage}[t]{0.30\textwidth}
            \begin{tcolorbox}[
                enhanced,
                height=5.6cm,
                colback=white,
                colframe=teal,
                coltitle=teal,
                fonttitle=\bfseries,
                title=Reproducibility,
                arc=2mm,
                boxrule=0.8pt,
                left=3mm,
                right=3mm,
                top=2mm,
                bottom=2mm
            ]
                \small\raggedright
                \begin{itemize}[leftmargin=1em,itemsep=0.35em]
                    \item Dependencies pinned in \texttt{requirements.txt}
                    \item MLflow tracking URI configured
                    \item Ray Train checkpoints saved
                    \item Random sources controlled with seeds
                \end{itemize}
            \end{tcolorbox}
        \end{minipage}
        \hfill
        \begin{minipage}[t]{0.30\textwidth}
            \begin{tcolorbox}[
                enhanced,
                height=5.6cm,
                colback=white,
                colframe=navy,
                coltitle=navy,
                fonttitle=\bfseries,
                title=Traceability,
                arc=2mm,
                boxrule=0.8pt,
                left=3mm,
                right=3mm,
                top=2mm,
                bottom=2mm
            ]
                \small\raggedright
                \begin{itemize}[leftmargin=1em,itemsep=0.35em]
                    \item Git repository initialized
                    \item Remote origin configured
                    \item Datasets available locally
                    \item Training configs logged with metrics
                \end{itemize}
            \end{tcolorbox}
        \end{minipage}
    \end{tcolorbox}
    \caption{Evidence of implemented logging, reproducibility, and traceability foundations.}
    \label{fig:foundations}
\end{figure}

\subsubsection{Why Logging Matters For This Project}

In this project, logging supports three important MLOps needs:

\begin{enumerate}
    \item \textbf{Debugging}: errors and warnings can be inspected after a failed run.
    \item \textbf{Traceability}: training and evaluation outputs can be connected to model runs.
    \item \textbf{Monitoring readiness}: logs can later be exported to tools such as Loki, Elasticsearch, or cloud logging services and visualized in Grafana.
\end{enumerate}

\subsubsection{Production Extension}

For the final version, logs can be connected to the monitoring stack. A practical production-style design would be:

\begin{lstlisting}[style=projectcode]
Application logs -> Loki or Elasticsearch -> Grafana dashboards
\end{lstlisting}

This will allow the report to include screenshots of API errors, model prediction logs, and system events once the serving and monitoring stages are implemented.

\subsection{Part 3: Reproducibility}

\subsubsection{Purpose}

Reproducibility means being able to recreate a previous result. In machine learning, this is difficult because results depend on code, data, dependencies, random seeds, training parameters, model artifacts, and hardware.

A reproducible ML project should answer:

\begin{itemize}
    \item Which code version produced this model?
    \item Which dataset was used?
    \item Which dependencies were installed?
    \item Which parameters were used?
    \item Which metrics were obtained?
    \item Where is the model artifact stored?
\end{itemize}

\subsubsection{Code Versioning With Git}

The repositories are Git repositories and are connected to a remote origin. Git provides history, collaboration, rollback, and traceability.

\begin{lstlisting}[style=projectcode]
origin  https://github.com/AyoubMoutik/mlops-project.git
\end{lstlisting}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth]{screenshots/github-repository.png}
    \caption{GitHub repository evidence showing the project remote, tracked files, and initial commit history.}
    \label{fig:github-repository}
\end{figure}

\subsubsection{Dependency Versioning}

The project includes:

\begin{lstlisting}[style=projectcode]
MLOpsFull/requirements.txt
\end{lstlisting}

This file pins or constrains many important dependencies, including:

\begin{itemize}
    \item \texttt{mlflow}
    \item \texttt{ray}
    \item \texttt{torch}
    \item \texttt{transformers}
    \item \texttt{scikit-learn}
    \item \texttt{fastapi}
    \item \texttt{pytest}
    \item \texttt{great-expectations}
\end{itemize}

This is necessary because dependency changes can alter model behavior or break the pipeline.

\subsubsection{Data Versioning}

For this educational project, the datasets are stored directly in the repository:

\begin{lstlisting}[style=projectcode]
MLOpsFull/datasets/dataset.csv
MLOpsFull/datasets/holdout.csv
MLOpsFull/datasets/projects.csv
MLOpsFull/datasets/tags.csv
\end{lstlisting}

This is acceptable because the datasets are small. In production, larger datasets should not usually be stored directly in Git. Better options include:

\begin{itemize}
    \item DVC;
    \item Git LFS;
    \item S3 or another object store;
    \item Snowflake or another warehouse;
    \item versioned data lake storage.
\end{itemize}

\subsubsection{Model And Experiment Versioning With MLflow}

The project configures MLflow tracking in \texttt{config.py}. The model registry path is derived from the configured storage directory, and MLflow is set using a file URI.

\begin{lstlisting}[style=projectcode,language=Python]
MODEL_REGISTRY = Path(f"{EFS_DIR}/mlflow")
MLFLOW_TRACKING_URI = "file://" + str(MODEL_REGISTRY.absolute())
mlflow.set_tracking_uri(MLFLOW_TRACKING_URI)
\end{lstlisting}

During training, the project uses an MLflow callback:

\begin{lstlisting}[style=projectcode,language=Python]
mlflow_callback = MLflowLoggerCallback(
    tracking_uri=MLFLOW_TRACKING_URI,
    experiment_name=experiment_name,
    save_artifact=True,
)
\end{lstlisting}

This supports experiment tracking by storing parameters, metrics, checkpoints, and artifacts.

\subsubsection{Checkpointing}

The training workflow creates checkpoints and keeps the best checkpoint based on validation loss:

\begin{lstlisting}[style=projectcode,language=Python]
checkpoint_config = CheckpointConfig(
    num_to_keep=1,
    checkpoint_score_attribute="val_loss",
    checkpoint_score_order="min",
)
\end{lstlisting}

This matters because the best model can be loaded later for prediction, evaluation, or deployment.

\subsubsection{Random Seeds}

The project contains a utility function for setting random seeds. This reduces randomness across NumPy, Python, PyTorch, CUDA, and hashing behavior.

\begin{lstlisting}[style=projectcode,language=Python]
def set_seeds(seed: int = 42):
    np.random.seed(seed)
    random.seed(seed)
    torch.manual_seed(seed)
    torch.cuda.manual_seed(seed)
    os.environ["PYTHONHASHSEED"] = str(seed)
\end{lstlisting}

This does not guarantee perfect reproducibility on every machine, but it is an important baseline.

\Needspace{20\baselineskip}
\subsubsection{Reproducibility Summary}

\begin{samepage}
\refstepcounter{table}
\begin{center}
\centering
\begin{tabularx}{0.96\textwidth}{>{\bfseries}p{0.27\textwidth} X}
\toprule
Requirement & Current Project Evidence \\
\midrule
Code versioning & Git repository with remote origin configured. \\
Dependency versioning & \texttt{requirements.txt}. \\
Data availability & CSV datasets in \texttt{MLOpsFull/datasets}. \\
Experiment tracking & MLflow tracking URI and Ray MLflow callback. \\
Model artifact tracking & Ray checkpoints and MLflow artifacts. \\
Configuration tracking & Training parameters logged with results. \\
Randomness control & \texttt{set\_seeds} utility. \\
\bottomrule
\end{tabularx}
\vspace{0.35em}

{\small Table~\thetable: Current reproducibility mechanisms in the project.}
\end{center}
\end{samepage}

\subsection{Part 4: CI/CD With Jenkins}

\subsubsection{Course Concept}

The CI/CD course material explains that machine learning systems should not depend on manual execution. Instead, workflows should be triggered by events such as pull requests, pushes to the main branch, new data, scheduled intervals, or detected performance regressions. The goal is to create a system that can continually improve while still giving the team control over when a model is promoted.

The ideal course workflow is:

\begin{lstlisting}[style=projectcode]
change code -> pull request -> model workloads -> quality check
           -> merge to main -> deployment workflow -> production service
\end{lstlisting}

In this project, this idea is implemented with Jenkins and Docker. The pipeline does not only test the code. It builds a reproducible image, trains a model, evaluates it, pushes the image to Docker Hub, deploys the API, and runs smoke checks.

\subsubsection{Docker-First CI/CD Architecture}

The final implementation uses a Docker-first strategy. Jenkins itself runs in Docker, but it is connected to the host Docker daemon so it can build application images, create short-lived workload containers, push images, and deploy the model service.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/cicd_pipeline.png}
    \caption{PlantUML diagram of the Dockerized Jenkins CI/CD pipeline.}
    \label{fig:cicd-pipeline}
\end{figure}

The corresponding PlantUML source is stored in:

\begin{lstlisting}[style=projectcode]
report/diagrams/cicd_pipeline.puml
\end{lstlisting}

\subsubsection{Files Added For CI/CD}

\begin{longtable}{p{0.31\textwidth} p{0.59\textwidth}}
\toprule
\textbf{File} & \textbf{Purpose} \\
\midrule
\texttt{Jenkinsfile} & Defines the CI/CD stages: checkout, branch resolution, Docker build, checks, training, evaluation, Docker Hub push, deployment, and artifact archiving. \\
\texttt{Dockerfile.jenkins} & Builds a Jenkins controller image with Docker CLI support. \\
\texttt{docker-compose.yml} & Starts Jenkins and later Prometheus/Grafana; mounts the Docker socket for Docker control. \\
\texttt{MLOpsFull/Dockerfile} & Builds the Python 3.10 ML application image. \\
\texttt{MLOpsFull/.dockerignore} & Keeps notebooks, caches, logs, artifacts, and local files out of Docker builds. \\
\bottomrule
\end{longtable}

\subsubsection{Jenkins Pipeline Stages}

The implemented pipeline contains these stages:

\begin{enumerate}
    \item \textbf{Checkout}: pulls the project source code from Git.
    \item \textbf{Resolve Branch}: normalizes branch names such as \texttt{origin/master} into \texttt{master}.
    \item \textbf{Build Docker Image}: builds the versioned application image and the \texttt{latest} tag.
    \item \textbf{CI Checks}: runs Python compilation, tests, and critical Flake8 rules inside the image.
    \item \textbf{Train}: trains the model in a container using Ray Train and MLflow.
    \item \textbf{Evaluate}: evaluates the trained run on the holdout dataset.
    \item \textbf{Push Image to Docker Hub}: publishes the image only after training and evaluation pass.
    \item \textbf{Deploy}: deploys the service only when \texttt{DEPLOY=true} and the branch is \texttt{master}.
    \item \textbf{Post Actions}: archives JSON, TXT, and LOG artifacts for evidence.
\end{enumerate}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth,height=0.45\textheight,keepaspectratio]{screenshots/build-successful.png}
    \caption{Jenkins build evidence showing all CI/CD stages completed successfully.}
    \label{fig:jenkins-build-successful}
\end{figure}

\subsubsection{Pipeline Parameters}

The Jenkins job is configurable so that the same pipeline can be used for fast demonstrations or heavier training runs.

\begin{table}[htbp]
\centering
\begin{tabularx}{0.96\textwidth}{>{\bfseries}p{0.23\textwidth} p{0.18\textwidth} X}
\toprule
Parameter & Default & Purpose \\
\midrule
\texttt{NUM\_SAMPLES} & \texttt{0} & Uses the full dataset for the Jenkins training run. \\
\texttt{NUM\_EPOCHS} & \texttt{6} & Controls training epochs. \\
\texttt{BATCH\_SIZE} & \texttt{16} & Controls model training batch size. \\
\texttt{MIN\_F1} & \texttt{0.85} & Minimum weighted F1 score required before deployment can continue. \\
\texttt{USE\_GPU} & \texttt{true} & Enables NVIDIA GPU acceleration for the training container when available. \\
\texttt{DEPLOY} & \texttt{true} & Enables or disables local deployment after evaluation. \\
\bottomrule
\end{tabularx}
\caption{Jenkins parameters used to control the CI/CD run.}
\end{table}

\subsubsection{Quality Gates}

The pipeline contains two important quality gates. The first one is the CI gate:

\begin{lstlisting}[style=projectcode]
python -m compileall madewithml
python -m pytest tests -q
python -m flake8 madewithml --select=E9,F63,F7,F82
\end{lstlisting}

This checks for syntax errors, runs the test suite, and limits Flake8 to critical categories that can break execution.

The second gate is the model evaluation gate. The Jenkinsfile reads the weighted F1 score from \texttt{eval\_results.json} and compares it with \texttt{MIN\_F1}. If the model fails the threshold, the pipeline stops before Docker Hub publishing and deployment.

\begin{infobox}{Model Quality Result}
The successful Dockerized Jenkins run produced a weighted F1 of \texttt{0.9054760519681985}. The model passed the configured \texttt{MIN\_F1=0.85} deployment gate and was promoted to the local serving stage.
\end{infobox}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth,height=0.45\textheight,keepaspectratio]{screenshots/dockerhub.png}
    \caption{Docker Hub evidence showing the pushed \texttt{ayoubmoutik/mlopsfull} image tags.}
    \label{fig:dockerhub-tags}
\end{figure}

\subsubsection{Containerized Stage Execution}

The helper function \texttt{dockerRun(...)} makes the CI, training, and evaluation stages run inside the built application image. Jenkins writes a temporary shell script, creates a temporary container, copies the script into the container, starts it, copies artifacts back from \texttt{/app/artifacts}, and removes the container.

This gives the pipeline one very important property: the checks and ML workloads run in the same type of environment that will later serve the model.

\subsubsection{Deployment And Smoke Tests}

The deployment stage starts the model service container:

\begin{lstlisting}[style=projectcode]
python -m madewithml.serve --run_id "$RUN_ID" --backend fastapi
\end{lstlisting}

The service exposes port \texttt{8000}. Jenkins validates deployment using smoke tests:

\begin{itemize}
    \item \texttt{GET /}: confirms that the service is reachable.
    \item \texttt{POST /predict/}: confirms that the model can return a prediction.
    \item \texttt{GET /metrics}: confirms that Prometheus metrics are exposed.
\end{itemize}

Smoke tests do not prove full correctness, but they catch the most important deployment failures: container crash, API startup failure, model loading failure, prediction failure, and missing metrics.

\subsubsection{CI/CD Evidence}

\begin{longtable}{p{0.35\textwidth} p{0.55\textwidth}}
\toprule
\textbf{Evidence} & \textbf{Meaning} \\
\midrule
\texttt{train\_results.json} & Training timestamp, run ID, parameters, and metric history. \\
\texttt{run\_id.txt} & MLflow run ID passed from training to evaluation and deployment. \\
\texttt{eval\_results.json} & Overall, per-class, and slice-level evaluation metrics. \\
\texttt{deploy\_health.json} & Response from the deployed API health endpoint. \\
\texttt{smoke\_response.json} & Prediction response from the deployment smoke test. \\
\texttt{metrics\_smoke.txt} & Prometheus-format metrics returned by the deployed API. \\
\texttt{deploy\_container.log} & Container logs captured when deployment fails. \\
\bottomrule
\end{longtable}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth,height=0.45\textheight,keepaspectratio]{screenshots/CI_CD_Evidence.png}
    \caption{Jenkins archived artifacts evidence showing training, evaluation, deployment health, smoke-test, metrics, and log outputs.}
    \label{fig:cicd-artifacts-evidence}
\end{figure}

\subsubsection{Model Serving}

\paragraph{Serving Backend Decision}

The original project supports Ray Serve, and Ray remains central to the project for Ray Data, Ray Train, Ray AIR checkpoints, and evaluation workloads. However, the final local Jenkins deployment uses FastAPI as the serving backend:

\begin{lstlisting}[style=projectcode]
python -m madewithml.serve --run_id <run_id> --backend fastapi
\end{lstlisting}

This decision was made because Ray Serve's HTTP proxy was unstable in the local Docker/Jenkins setup. FastAPI provides a reliable HTTP layer for the demonstration while preserving Ray for the ML workflow. Ray Serve remains available through:

\begin{lstlisting}[style=projectcode]
python -m madewithml.serve --run_id <run_id> --backend ray-serve
\end{lstlisting}

\paragraph{Model Lifecycle}

\begin{figure}[H]
    \centering
    \includegraphics[height=0.52\textheight,keepaspectratio]{figures/ml_lifecycle.png}
    \caption{PlantUML diagram of the model lifecycle from CSV data to monitored API.}
    \label{fig:ml-lifecycle}
\end{figure}

The serving process loads the best checkpoint for the Jenkins-generated \texttt{run\_id}. The checkpoint includes the model weights, model arguments, and metadata such as the class-to-index mapping. This allows the API to preprocess incoming requests in the same way the training pipeline did.

\paragraph{Prediction API}

The main endpoint is:

\begin{lstlisting}[style=projectcode]
POST http://localhost:8000/predict/
\end{lstlisting}

Example payload:

\begin{lstlisting}[style=projectcode]
{
  "title": "Text classification with transformers",
  "description": "A project using BERT for NLP classification"
}
\end{lstlisting}

The API returns prediction results and monitoring information. Low-confidence predictions are mapped to the fallback class \texttt{other}, which helps avoid pretending that the model is confident when it is not.

\subsection{Part 5: Monitoring}

\subsubsection{Course Concept}

The monitoring course material explains that the real work begins after deployment. Traditional software is usually deterministic: if the code is correct, the same input should produce the same intended behavior. Machine learning systems are probabilistic and depend on the data distribution. A model can degrade when production data becomes different from training data.

The course separates monitoring into several layers:

\begin{itemize}
    \item \textbf{System health}: latency, throughput, error rates, CPU, GPU, and memory.
    \item \textbf{Model performance}: accuracy, precision, recall, F1, and business metrics.
    \item \textbf{Outliers}: individual unusual inputs.
    \item \textbf{Drift}: distribution-level changes between reference data and production windows.
    \item \textbf{Response workflow}: alert, inspect, and act.
\end{itemize}

The project implements the educational foundation for these ideas through Prometheus metrics, Grafana dashboards, model behavior tracking, and simple drift helper functions.

\subsubsection{Monitoring Architecture}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/monitoring_architecture.png}
    \caption{PlantUML diagram of the local monitoring architecture.}
    \label{fig:monitoring-architecture}
\end{figure}

The local architecture contains four main services:

\begin{longtable}{p{0.28\textwidth} p{0.25\textwidth} p{0.37\textwidth}}
\toprule
\textbf{Service} & \textbf{URL} & \textbf{Role} \\
\midrule
Jenkins & \texttt{http://localhost:8080} & Builds, tests, trains, evaluates, pushes, and deploys. \\
Model API & \texttt{http://localhost:8000} & Serves predictions and exposes metrics. \\
Prometheus & \texttt{http://localhost:9090} & Scrapes \texttt{/metrics} from the model API. \\
Grafana & \texttt{http://localhost:3000} & Visualizes API and model behavior metrics. \\
\bottomrule
\end{longtable}

\subsubsection{Metrics Endpoint}

The model service exposes:

\begin{lstlisting}[style=projectcode]
GET /metrics
\end{lstlisting}

This endpoint is implemented in \texttt{serve.py} using \texttt{monitoring\_metrics.render\_metrics()}. It returns Prometheus-format metrics, which Prometheus scrapes from the Docker network target:

\begin{lstlisting}[style=projectcode]
mlopsfull-serve:8000/metrics
\end{lstlisting}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth,height=0.42\textheight,keepaspectratio]{screenshots/prometheus_targets_up.png}
    \caption{Prometheus target evidence showing the model API metrics endpoint in the \texttt{UP} state.}
    \label{fig:prometheus-targets-up}
\end{figure}

\subsubsection{Metrics Collected}

\begin{longtable}{p{0.42\textwidth} p{0.48\textwidth}}
\toprule
\textbf{Metric} & \textbf{Meaning} \\
\midrule
\texttt{mlopsfull\_requests\_total} & Number of HTTP requests by endpoint, method, and status. \\
\texttt{mlopsfull\_request\_latency\_seconds} & Latency distribution for prediction and evaluation endpoints. \\
\texttt{mlopsfull\_predictions\_total} & Number of predictions emitted per predicted class. \\
\texttt{mlopsfull\_prediction\_confidence} & Histogram of prediction confidence values. \\
\texttt{mlopsfull\_input\_text\_length\_tokens} & Histogram of input title and description lengths. \\
\texttt{mlopsfull\_other\_prediction\_rate} & Latest rate of predictions mapped to the fallback \texttt{other} class. \\
\texttt{mlopsfull\_input\_validation}\newline\texttt{\_failures\_total} & Count of empty title or description validation failures. \\
\bottomrule
\end{longtable}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth,height=0.45\textheight,keepaspectratio]{screenshots/garafana_dashboard.png}
    \caption{Grafana dashboard evidence visualizing API request rate, latency, predictions, confidence, and input validation metrics.}
    \label{fig:grafana-dashboard}
\end{figure}

\subsubsection{Monitoring Helper Logic}

The file \texttt{monitoring.py} contains logic for:

\begin{itemize}
    \item calculating request text length;
    \item validating empty title and description fields;
    \item extracting prediction confidence;
    \item summarizing prediction outputs;
    \item detecting text length drift;
    \item detecting class distribution drift.
\end{itemize}

These helpers reflect the monitoring PDF's distinction between health checks and model behavior checks. Health checks answer whether the API is running. Model behavior checks answer whether the model is behaving usefully.

\subsubsection{Drift Foundation}

The project includes two lightweight drift checks:

\begin{lstlisting}[style=projectcode]
detect_text_length_drift(reference_lengths, current_lengths)
detect_class_distribution_drift(reference_classes, current_classes)
\end{lstlisting}

The first compares average text length between a reference window and a current window. The second compares class distributions using total variation distance. These are intentionally simple and educational. In production, this could be extended with tools such as EvidentlyAI, WhyLogs, Alibi Detect, or managed platforms.

\subsubsection{Traffic Generation Result}

A traffic script sends repeated prediction requests to populate Prometheus and Grafana:

\begin{lstlisting}[style=projectcode]
python scripts/send_monitoring_traffic.py --url http://localhost:8000/predict/ --requests 200
\end{lstlisting}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.96\textwidth,height=0.45\textheight,keepaspectratio]{screenshots/send_monitoring_traffic_script.png}
    \caption{Traffic generation evidence showing repeated prediction requests sent to the deployed API.}
    \label{fig:monitoring-traffic-script}
\end{figure}

The monitoring traffic run produced:

\begin{lstlisting}[style=projectcode]
Total requests: 200
HTTP 200 responses: 200
HTTP errors: 0
\end{lstlisting}

The observed model behavior was consistent with the evaluation result. The deployed model produced successful predictions across the target classes, including \texttt{natural-language-processing}, \texttt{computer-vision}, \texttt{mlops}, and \texttt{other}. Prometheus recorded \texttt{230} prediction requests in total, with an average monitored confidence of approximately \texttt{0.9285}. This shows that monitoring captures both API health and model behavior after deployment.

\subsubsection{Alert, Inspect, Act}

The course monitoring workflow is:

\begin{lstlisting}[style=projectcode]
alert -> inspect -> act
\end{lstlisting}

The current project implements the measurement layer needed before alerting. A production extension could add Prometheus alert rules such as:

\begin{itemize}
    \item alert if request errors increase;
    \item alert if prediction latency crosses a threshold;
    \item alert if \texttt{other} prediction rate remains high;
    \item alert if average confidence drops below a threshold;
    \item alert if text length or class distribution drift is detected.
\end{itemize}

After alerting, the team should inspect metadata, thresholds, logs, reference data, current data, and model outputs before acting. Possible actions include checking data expectations, confirming that the schema did not change, retraining the model, updating the reference window, or investigating whether outliers are valid new production examples.

\section{Limitations And Future Improvements}

\subsection{Current Limitations}

\begin{itemize}
    \item The monitoring setup is local and Docker-based.
    \item The training dataset is small and stored locally for this educational project.
    \item The deployment gate is configurable and currently enforces a stricter weighted F1 threshold.
    \item Drift helpers exist, but they are not yet scheduled as recurring production jobs.
    \item No Alertmanager, Slack, email, or PageDuty alert routing is configured.
    \item Ray Serve remains available, but FastAPI was selected for stable local deployment.
\end{itemize}

\subsection{Future Improvements}

\begin{itemize}
    \item Add more labeled data and scheduled retraining for broader generalization.
    \item Calibrate deployment thresholds as production requirements evolve.
    \item Add Prometheus alert rules and Alertmanager notifications.
    \item Run drift checks on scheduled windows or event streams.
    \item Store prediction events for later audit and retraining analysis.
    \item Add DVC or object storage for larger dataset versioning.
    \item Deploy to Kubernetes or a managed ML platform for a more production-like environment.
\end{itemize}

\section{Conclusion}

This project implements an end-to-end MLOps workflow for a text classification model. The first foundation is software structure: notebook logic is separated into reusable Python modules for data processing, modeling, training, tuning, evaluation, prediction, serving, and monitoring. The second foundation is reproducibility: Git, dependency files, MLflow, Ray checkpoints, deterministic utilities, and archived artifacts make the workflow traceable.

The CI/CD implementation extends the project from a local ML codebase into an automated delivery workflow. Jenkins builds the Docker image, runs code checks, trains the model, evaluates it, gates deployment with weighted F1, publishes the image, deploys the API, and archives evidence. The monitoring implementation then connects the deployed API to Prometheus and Grafana, exposing both system behavior and model behavior metrics.

The final result is not only a trained model, but a working MLOps system: code changes can trigger automated validation, model training can produce tracked artifacts, deployment is guarded by evaluation, and the running service can be observed after release. This is the central lesson of the project: machine learning engineering is not just about producing a model, but about building the operational workflow that keeps the model reliable, traceable, and improvable.

\end{document}
